diff --git a/sinaps-backend/controllers/agentController.js b/sinaps-backend/controllers/agentController.js
index 583dae7..acd3f8d 100644
--- a/sinaps-backend/controllers/agentController.js
+++ b/sinaps-backend/controllers/agentController.js
@@ -50,7 +50,7 @@ exports.getAgents = async (req, res) => {
 
 exports.approveAgent = async (req, res) => {
   try {
-    const agent = await Agent.findByIdAndUpdate(req.params.id, { status: 'approved' }, { new: true });
+    const agent = await Agent.findByIdAndUpdate(req.params.id, { status: 'approved' }, { returnDocument: 'after' });
     res.json(agent);
   } catch (error) {
     res.status(500).json({ error: error.message });
diff --git a/sinaps-backend/controllers/conversationController.js b/sinaps-backend/controllers/conversationController.js
index 0114a6a..c8d3d4d 100644
--- a/sinaps-backend/controllers/conversationController.js
+++ b/sinaps-backend/controllers/conversationController.js
@@ -66,7 +66,7 @@ exports.escalateConversation = async (req, res) => {
     const conversation = await Conversation.findByIdAndUpdate(
       req.params.id,
       { handledBy: 'humain', status: 'en_attente' },
-      { new: true }
+      { returnDocument: 'after' }
     )
       .populate('client', 'name avatar email')
       .populate('assignedAgent', 'name avatar');
@@ -91,7 +91,7 @@ exports.assignAgent = async (req, res) => {
         handledBy: 'humain',
         status: 'en_cours',
       },
-      { new: true }
+      { returnDocument: 'after' }
     )
       .populate('client', 'name avatar email')
       .populate('assignedAgent', 'name avatar');
@@ -122,7 +122,7 @@ exports.closeConversation = async (req, res) => {
     const conversation = await Conversation.findByIdAndUpdate(
       req.params.id,
       update,
-      { new: true, runValidators: true }
+      { returnDocument: 'after', runValidators: true }
     )
       .populate('client', 'name avatar email')
       .populate('assignedAgent', 'name avatar');
diff --git a/sinaps-backend/controllers/messageController.js b/sinaps-backend/controllers/messageController.js
index f6c8d60..79c2f15 100644
--- a/sinaps-backend/controllers/messageController.js
+++ b/sinaps-backend/controllers/messageController.js
@@ -18,7 +18,7 @@ exports.sendMessage = async (req, res) => {
     const updatedConv = await Conversation.findByIdAndUpdate(
       conversationId,
       { updatedAt: new Date() },
-      { new: true }
+      { returnDocument: 'after' }
     )
       .populate('client', 'name avatar email')
       .populate('assignedAgent', 'name avatar');
@@ -42,7 +42,7 @@ exports.sendMessage = async (req, res) => {
         const reUpdatedConv = await Conversation.findByIdAndUpdate(
           conversationId,
           { updatedAt: new Date() },
-          { new: true }
+          { returnDocument: 'after' }
         )
           .populate('client', 'name avatar email')
           .populate('assignedAgent', 'name avatar');
