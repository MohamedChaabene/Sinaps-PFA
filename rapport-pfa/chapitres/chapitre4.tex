\chapter{Réalisation}

Ce chapitre décrit l'implémentation \textbf{effective} : organisation du code, choix techniques, points d'implémentation notables et écrans illustrés par les captures intégrées.

\section{Environnement et choix techniques}

\begin{table}[H]
\centering
\caption{Stack réellement utilisée}
\label{tab:stack}
\begin{tabularx}{\textwidth}{|l|X|}
\tablelines
\toprule
Catégorie & Choix réellement utilisés \\
\midrule
Frontend Web & Next.js 16 (App Router), React 19 et TypeScript \\
Backend / API & Node.js et Express 5, avec une API REST/JSON \\
Application mobile & React Native 0.86, Expo 57 et TypeScript \\
Données & MongoDB avec Mongoose 9 ; MongoDB Atlas pour l'environnement distant documenté \\
IA / RAG & Google Gemini et retriever local fondé sur TF-IDF et la similarité cosinus \\
Communication temps réel & Socket.io côté backend, Web et mobile \\
Authentification & JWT pour les sessions et Google OAuth pour l'identification client \\
Tests & Jest, Supertest et MongoDB Memory Server ; Postman est mentionné dans la documentation pour les vérifications manuelles de l'API \\
Versionnement / déploiement & Git et GitHub ; Vercel pour le Web et Render pour le backend \\
\bottomrule
\end{tabularx}
\end{table}

La figure~\ref{fig:sinaps-technologies} présente visuellement les principales technologies mobilisées dans cette stack.

\begin{figure}[H]
\centering
\begin{tabular}{@{}cccc@{}}
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/nextjs.png} &
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/react.png} &
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/nodejs.png} &
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/github.png} \\
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/mongodb.png} &
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/gemini.png} &
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/socketio.png} &
  \includegraphics[height=1.25cm,keepaspectratio]{images/branding/expo.png}
\end{tabular}
\caption{Principales technologies mobilisées dans la réalisation de SINAPS}
\label{fig:sinaps-technologies}
\end{figure}

\textbf{Lecture de la stack.} Le terme \textbf{MERN} désigne ici l'association MongoDB, Express.js, React et Node.js. Next.js est le framework utilisé autour de l'application React web ; TypeScript apporte un typage statique aux clients web et mobile. Express.js expose l'API REST du backend Node.js, avec des échanges JSON, tandis que MongoDB assure la persistance des utilisateurs, agents, conversations et messages. Mongoose définit les schémas utilisés par le backend.

\textbf{Client mobile.} React Native et Expo constituent une application séparée du frontend Web, partageant le backend REST et Socket.io. Cette stack mobile n'est donc pas une composante supplémentaire de MERN.

\textbf{IA et recherche documentaire.} Le service Google Gemini génère les réponses lorsqu'il est disponible. Avant cette génération, le retriever local normalise les textes, calcule des scores TF-IDF et compare les vecteurs par similarité cosinus afin de sélectionner les éléments pertinents de la base de connaissances. En l'absence de réponse Gemini exploitable, le backend dispose également d'un repli local.

\textbf{Temps réel et authentification.} Socket.io complète l'API REST pour diffuser les messages et les mises à jour de conversations. Google OAuth intervient dans l'identification client, tandis que des jetons JWT représentent les sessions client, agent et administrateur et sont contrôlés par les middleware du backend.

\textbf{Tests et variantes MongoDB.} Jest orchestre les tests, Supertest vérifie les routes HTTP et MongoDB Memory Server fournit une base isolée pour les tests automatisés. \textbf{MongoDB} désigne la technologie de persistance ; \textbf{MongoDB Atlas} désigne le service distant documenté pour l'environnement de production ; \textbf{MongoDB Memory Server} est réservé à l'isolation des tests. Git et GitHub assurent le versionnement et l'hébergement du dépôt. Postman est mentionné dans la documentation du projet pour les vérifications manuelles de l'API ; aucun compte rendu détaillé ni collection Postman n'est conservé dans le dépôt.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.38\textheight,keepaspectratio]{images/architecture/SINAPS Architecture Diagram.png}
\caption{Architecture générale et stack technologique de SINAPS}
\label{fig:architecture-stack}
\end{figure}

\textbf{Justification succincte.} Next.js fournit le routage et un frontend React moderne, conformément à l'orientation \og React \fg{} du sujet. Express est l'API demandée côté Node. MongoDB Atlas évite d'administrer un serveur local de production. Gemini a été retenu (et utilisé effectivement) plutôt qu'OpenAI. Socket.io couvre le besoin de réactivité du chat et de la file agent. JWT sépare clairement sessions client et sessions agent.

\subsection{Déploiement et hébergement}

La chaîne de déploiement vérifiée est \textbf{GitHub $\rightarrow$ Vercel (Web)} et \textbf{GitHub $\rightarrow$ Render (Backend)}. Le frontend public est accessible à \texttt{https://sinaps-pfa.vercel.app} et utilise l'API Render \texttt{https://sinaps-backend-jm1h.onrender.com/api}. L'endpoint \texttt{GET /api/health} a répondu avec le statut HTTP 200, puis une identification par nom et e-mail, la création d'une conversation et un échange avec réponse IA ont été vérifiés depuis le Web. Cette vérification fonctionnelle a été réalisée par une exécution externe ; la configuration complète Vercel/Render et les secrets ne sont pas versionnés et ne sont donc pas reproductibles depuis le dépôt seul. Elle ne constitue pas une preuve de fonctionnement natif Android ou iOS.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.38\textheight,keepaspectratio]{images/architecture/Architecture de déploiement SINAPS.png}
\caption{Architecture de déploiement et d'hébergement de SINAPS}
\label{fig:deployment-architecture}
\end{figure}

Pour la persistance distante documentée, MongoDB Atlas est fourni au backend via \texttt{MONGO\_URI}. Le code de connexion accepte également une base MongoDB locale ou une base en mémoire pour le développement et les tests ; ces variantes ne doivent pas être confondues avec Atlas. La vérification effectuée dans ce rapport porte sur l'API publique et le parcours Web ; les paramètres privés de la base distante ne sont pas exposés.

Les alternatives du sujet (Vue.js, Flutter, Firebase, Auth0, Kafka, LangChain) \textbf{n'ont pas été mises en œuvre}.

\section{Organisation du code}

Le dépôt est organisé autour de trois sous-projets : frontend web, backend et client mobile.

\begin{table}[H]
\centering
\caption{Arborescence simplifiée}
\label{tab:tree}
\begin{tabularx}{\textwidth}{|l|X|}
\tablelines
\toprule
Chemin & Rôle \\
\midrule
\texttt{sinaps-project/app/} & Routes : \texttt{/}, \texttt{/login}, \texttt{/agent}, \texttt{/agent/signup}, \texttt{/admin} \\
\texttt{sinaps-project/components/chat/} & Chat client (fil, en-tête, compositeur, satisfaction, prompts) \\
\texttt{sinaps-project/components/admin/} & Tableau de bord administrateur \\
\texttt{sinaps-project/lib/api.ts} & Client HTTP et gestion des jetons \\
\texttt{sinaps-project/lib/socket.ts} & Client Socket.io \\
\texttt{sinaps-backend/server.js} & HTTP + Socket.io, CORS, montage des routes \\
\texttt{sinaps-backend/models/} & Schémas Mongoose \\
\texttt{sinaps-backend/controllers/} & Logique métier \\
\texttt{sinaps-backend/middleware/auth.js} & Gardes JWT \\
\texttt{sinaps-backend/services/} & \texttt{ragService.js}, \texttt{geminiService.js} \\
\texttt{sinaps-backend/data/knowledgeBase.js} & FAQ d'exemple \\
\texttt{sinaps-backend/tests/} & Suites Jest \\
\texttt{sinaps-backend/seed.js} & Jeu de démonstration \\
\texttt{sinaps-mobile/src/} & Client Expo / React Native : authentification, chat, temps réel, écrans agent et administrateur \\
\bottomrule
\end{tabularx}
\end{table}

Le frontend écoute par défaut \texttt{NEXT\_PUBLIC\_API\_URL=http://localhost:5000/api}. Le backend écoute le port \textbf{5000}. Un endpoint \texttt{GET /api/health} permet de vérifier que l'API est joignable. Aucun manifeste spécifique à Vercel ou Render n'est présent dans le dépôt ; leurs paramètres de déploiement sont documentés dans le README et reposent sur les variables d'environnement indiquées.

\section{Backend : API REST, middleware et WebSockets}

\subsection{Routes}

\begin{table}[H]
\centering
\caption{Principales routes HTTP}
\label{tab:api}
\small
\begin{tabularx}{\textwidth}{|l|l|X|}
\tablelines
\toprule
Méthode & Chemin & Rôle \\
\midrule
GET & \texttt{/api/health} & Santé du serveur \\
POST & \texttt{/api/users/find-or-create} & Session client (Google ou e-mail) \\
POST & \texttt{/api/conversations/find-or-create} & Conversation active du client \\
GET & \texttt{/api/conversations} & Liste (agent/admin), filtre \texttt{status}, \texttt{search} \\
GET & \texttt{/api/conversations/:id} & Fil + messages (propriétaire ou staff) \\
PATCH & \texttt{/api/conversations/:id/escalate} & Vers humain \\
PATCH & \texttt{/api/conversations/:id/de-escalate} & Retour IA \\
PATCH & \texttt{/api/conversations/:id/assign} & Assignation d'agent \\
PATCH & \texttt{/api/conversations/:id/close} & Clôture $\pm$ satisfaction \\
POST & \texttt{/api/messages} & Envoi ; déclenche l'IA si besoin \\
POST & \texttt{/api/conversations/:id/quick-reply} & Exécution d'une action rapide \\
POST & \texttt{/api/upload} & Fichier (session requise) \\
POST & \texttt{/api/agents/signup} & Inscription \texttt{pending} \\
POST & \texttt{/api/agents/login} & JWT staff \\
GET & \texttt{/api/agents} & Liste (admin) \\
PATCH & \texttt{/api/agents/:id/approve} & Validation \\
DELETE & \texttt{/api/agents/:id} & Rejet (suppression) \\
GET & \texttt{/api/stats} & Indicateurs admin \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Temps réel}

Lors d'une connexion Socket.io, le client peut \texttt{join\_conversation} ou \texttt{leave\_conversation}. Le backend émet \texttt{message\_received} et \texttt{conversation\_updated} vers la salle. Les événements globaux \texttt{conversation\_created} et \texttt{conversation\_updated} servent au rafraîchissement des listes.

\subsection{Statistiques}

\texttt{getStats} calcule le nombre total de conversations, les résolutions par IA ou par un humain, la moyenne des notes renseignées et un \textbf{temps de réponse moyen} à partir des messages.

\subsection{Téléversement}

Multer enregistre les fichiers avec un nom unique. Types acceptés : images, vidéos, PDF et Word. La taille maximale est configurable (\texttt{MAX\_UPLOAD\_SIZE\_MB}, défaut 15~Mo) et le type MIME déclaré est filtré.

\section{Moteur IA}

Le fichier \texttt{geminiService.js} enchaîne : retrieval $\rightarrow$ construction du prompt $\rightarrow$ essai de modèles Gemini $\rightarrow$ repli RAG textuel. La FAQ d'exemple couvre notamment : suivi de commande, retard, remboursement, mot de passe, code promo. Elle n'est pas un corpus métier confidentiel de SINAPS TEC.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.38\textheight,keepaspectratio]{images/architecture/Pipeline RAG de SINAPS.png}
\caption{Pipeline de recherche augmentée par génération (RAG) de SINAPS}
\label{fig:rag-pipeline}
\end{figure}

Le retriever normalise le français (minuscules, accents, apostrophes et ponctuation), tokenise les textes, retire des mots vides et applique une normalisation de variations lexicales. Il calcule ensuite des vecteurs TF-IDF et plusieurs similarités cosinus : question (poids 0,5), mots-clés (0,3), texte combiné (0,1) et réponse (0,05), avec un bonus pour les mots-clés pertinents. Seuls les documents dont le score est strictement supérieur à \textbf{0,35} sont retenus, avec un maximum de \textbf{deux documents} (\texttt{topK = 2}). Les demandes de facturation sont limitées aux documents de catégorie \texttt{billing}. Sans clé Gemini, en cas d'échec du modèle ou sans document pertinent, le service utilise la meilleure réponse locale ou propose une mise en relation humaine.

\section{Frontend}

\subsection{Espace client}

Après identification (\texttt{ClientEntryForm}, bouton Google et/ou formulaire), \texttt{SupportChatApp} charge le fil, affiche \texttt{ChatThread}, \texttt{QuickPrompts}, \texttt{MessageComposer}, et les actions d'escalade / désescalade dans \texttt{ChatHeader}. Un dialogue de satisfaction s'ouvre à la clôture.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{images/screenshots/web/sinaps-satisfaction-dialog.png}
  \caption{Évaluation de la satisfaction à la clôture d'une conversation}
  \label{fig:satisfaction-dialog}
\end{figure}

Les réponses IA peuvent porter des \textbf{actions rapides} : confirmer que la réponse a suffi, poser une autre question ou demander un agent humain. Une action est envoyée comme message client et son état de chargement est affiché ; les boutons ne sont proposés que sur le dernier message IA éligible. Le compositeur est verrouillé pendant la génération IA afin d'éviter les doublons.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{images/screenshots/web/sinaps-client-chat-ai.png}
  \caption{Conversation client avec réponse RAG/Gemini et actions rapides}
  \label{fig:client-chat-ai}
\end{figure}

\subsection{Espace agent}

La page \texttt{/agent} est protégée (\texttt{AuthGuard}). L'agent se connecte sur \texttt{/login}. L'inscription se fait sur \texttt{/agent/signup} (compétences en texte). Tant que le compte est \texttt{pending}, le login renvoie une erreur 403.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{images/screenshots/web/sinaps-human-escalation.png}
  \caption{Escalade d'une conversation vers la file des agents}
  \label{fig:human-escalation}
\end{figure}

\subsection{Espace administrateur}

Le tableau de bord affiche les indicateurs opérationnels de satisfaction et de délai de réponse, la liste des agents à valider (compétences en badges), et un historique avec recherche (nom / e-mail client) et onglets de statut. Les conversations marquées comme données de test sont exclues des indicateurs et des listes opérationnelles par défaut. Les dernières corrections ont également stabilisé le chargement concurrent des conversations, le rendu des horodatages et les avatars de repli côté client.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{images/screenshots/web/sinaps-admin-dashboard.png}
  \caption{Tableau de bord administrateur de SINAPS TEC}
  \label{fig:admin-dashboard}
\end{figure}

  \subsection{Client mobile}

  Le dossier \texttt{sinaps-mobile/} contient un client Expo / React Native connecté aux mêmes routes HTTP et au même serveur Socket.io que le client web. Il propose l'authentification client par nom / e-mail ou Google, l'authentification agent / administrateur par e-mail et mot de passe, la persistance de session, le chat, les pièces jointes, l'indicateur de frappe, l'escalade, le retour vers l'IA, la clôture avec satisfaction et le mode sombre. Des écrans distincts permettent également à un agent de consulter la file, de prendre en charge une conversation, de répondre et de la résoudre, ainsi qu'à un administrateur de consulter les statistiques, les agents et l'historique.

  Le mobile n'est pas visuellement identique au web, car son interface est adaptée aux petits écrans. Il reprend toutefois des actions rapides \textbf{contextuelles} attachées à certaines réponses IA, l'escalade vers un agent humain, le retour vers l'IA, la clôture avec satisfaction et l'indicateur d'état Socket.io. Son compositeur propose les images et documents ; le parcours vidéo exposé par le web n'a pas été ajouté. L'inscription d'un agent reste disponible uniquement sur \texttt{/agent/signup}. La connexion Google mobile dépend en outre d'une build native Expo configurée. Enfin, les valeurs de secours prévues dans l'écran administrateur mobile lorsque les statistiques sont absentes ne doivent pas être considérées comme des mesures de production.

  Le client mobile a été lancé en Expo Web sur \texttt{http://localhost:8081} avec l'API Render. Le parcours vérifié comprend l'identification directe, l'envoi d'un message, une réponse IA fondée sur le RAG et les actions rapides contextuelles, ainsi que l'indicateur Socket.io \og En direct \fg{}. Une escalade explicite vers un agent humain fait apparaître les états \og En attente \fg{} et \og Agent demandé \fg{} ; le contrôle \og Mode IA \fg{} permet ensuite le retour vers l'assistant, avec une nouvelle réponse IA et des actions rapides. La clôture déclenche enfin le dialogue d'évaluation à cinq étoiles. Cette vérification ne couvre ni une build native Android/iOS ni Google Sign-In natif. Le dépôt contient la configuration TypeScript du client mobile, mais aucune suite de tests automatisés mobile n'est présente.

  L'écran d'identification directe par nom et e-mail est présenté en figure~\ref{fig:mobile-login}. La figure~\ref{fig:mobile-chat-ai} montre ensuite une conversation mobile avec une réponse de l'assistant IA fondée sur le RAG et les actions rapides disponibles pour le client.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.38\textwidth]{images/screenshots/mobile/sinaps-mobile-login.png}
    \caption{Écran d'identification directe du client dans l'application mobile SINAPS}
    \label{fig:mobile-login}
  \end{figure}

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.38\textwidth]{images/screenshots/mobile/sinaps-mobile-chat-ai.png}
    \caption{Conversation mobile avec réponse de l'assistant IA fondée sur le RAG et actions rapides}
    \label{fig:mobile-chat-ai}
  \end{figure}

  La figure~\ref{fig:mobile-human-escalation} illustre la demande explicite de prise en charge humaine, les états \og En attente \fg{} et \og Agent demandé \fg{}, ainsi que le contrôle de retour \og Mode IA \fg{}. Après la résolution, la figure~\ref{fig:mobile-satisfaction} présente le dialogue de notation et de clôture de la conversation.

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.38\textwidth]{images/screenshots/mobile/sinaps-mobile-human-escalation.png}
    \caption{Escalade explicite vers un agent humain : états En attente, Agent demandé et retour Mode IA}
    \label{fig:mobile-human-escalation}
  \end{figure}

  \begin{figure}[H]
    \centering
    \includegraphics[width=0.38\textwidth]{images/screenshots/mobile/sinaps-mobile-satisfaction.png}
    \caption{Évaluation de la satisfaction et clôture d'une conversation mobile}
    \label{fig:mobile-satisfaction}
  \end{figure}

  \section{Données de démonstration}

Le script \texttt{npm run seed} recrée des comptes de démonstration avec un mot de passe défini par le script de seed, ainsi que des utilisateurs et conversations de démonstration. Ces comptes servent aux captures et aux tests manuels ; ils constituent uniquement des données locales ou de test et non une population réelle de production.

\section{Mise en route (développement local)}

\begin{lstlisting}[caption={Démarrage local (d'après le README du projet)},label=lst:run]
# MongoDB Atlas : renseigner MONGO_URI
cd sinaps-backend && npm install && npm run seed && npm run dev
cd sinaps-project && npm install && npm run dev
\end{lstlisting}

Variables nécessaires côté backend : \texttt{PORT}, \texttt{MONGO\_URI}, \texttt{JWT\_SECRET}, \texttt{GEMINI\_API\_KEY}, \texttt{GOOGLE\_CLIENT\_ID} ; côté frontend : \texttt{NEXT\_PUBLIC\_API\_URL}, \texttt{NEXT\_PUBLIC\_GOOGLE\_CLIENT\_ID}. Sans clés Google/Gemini, des modes dégradés existent (identification directe, réponses RAG locales). Le dépôt confirme l'intégration de Google Sign-In, Gemini et MongoDB Atlas, mais ne fournit pas de compte rendu détaillé d'une recette manuelle en conditions réelles.
