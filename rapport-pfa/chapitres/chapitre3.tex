\chapter{Conception du système}

\section{Introduction}

Ce chapitre présente les choix de conception retenus pour transformer les besoins du chapitre~2 en une architecture, des modèles de données et des scénarios de fonctionnement cohérents. Les diagrammes décrivent le \textbf{système implémenté} et distinguent les responsabilités des clients, du backend, des services internes et des acteurs humains.

\section{Architecture générale}

Le système est une architecture composée de deux clients (web et mobile) et d'un backend commun, communiquant par HTTP(S) et WebSocket, avec une base documentaire MongoDB. Le client web repose sur Next.js / React et le client mobile sur Expo / React Native. Le backend expose une API REST/JSON, coordonne l'authentification, les conversations, le moteur RAG/Gemini et les échanges temps réel avec Socket.io. Ces choix s'appuient sur les documentations respectives de Next.js, Express, MongoDB et Socket.io \cite{nextjs,express,mongodb,socketio}.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.62\textheight,keepaspectratio]{images/architecture/sinaps-logical-architecture.png}
\caption{Architecture logique d'Op Support IA (vue déployable en local)}
\label{fig:archi}
\end{figure}

Les flux principaux sont :

\begin{enumerate}[leftmargin=1.6em]
  \item le navigateur charge l'interface Next.js ;
  \item les appels métier passent par l'API Express ;
  \item après un message client, si la conversation est en mode IA, le backend appelle le retriever puis Gemini (ou le repli) et enregistre un message \texttt{ia} ;
  \item Socket.io notifie la salle de conversation et, pour les listes, un événement global ;
  \item les fichiers sont stockés sur disque sous \texttt{uploads/} et servis en statique ;
  \item le client peut demander une prise en charge humaine ; l'agent traite alors la conversation depuis sa file.
\end{enumerate}

\section{Architecture logicielle}

L'organisation logique repose sur quatre ensembles complémentaires :

\begin{itemize}[leftmargin=1.4em]
  \item les \textbf{clients}, web et mobile, qui présentent les interfaces de conversation, d'agent et d'administration et communiquent avec le backend ;
  \item le \textbf{backend}, qui expose l'API REST, applique les contrôles d'accès, persiste les données et coordonne les changements d'état ;
  \item les \textbf{services internes}, qui regroupent l'authentification, le retriever RAG, la génération Gemini, le téléversement et les notifications Socket.io ;
  \item la \textbf{persistance et les services externes}, constitués de MongoDB pour les entités métier, de Google OAuth pour l'identification client et de Gemini pour la génération lorsqu'une clé valide est disponible.
\end{itemize}

Cette séparation permet aux deux clients de partager les mêmes conversations, messages et règles d'autorisation sans dupliquer la logique métier. Les composants techniques restent distincts des acteurs humains identifiés au chapitre~2.

\section{Diagramme de cas d'utilisation}

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/sinaps-use-cases.png}
\caption{Diagramme global des cas d'utilisation de SINAPS}
\label{fig:cu}
\end{figure}

\subsection{Authentification commune}

Le cas \textbf{S'authentifier} est commun aux actions visibles des trois acteurs. Le client fournit un \textbf{jeton Google}, vérifié avec \texttt{GOOGLE\_CLIENT\_ID}, ou un couple nom / e-mail. Le serveur trouve ou crée le profil client et délivre un JWT de rôle \texttt{client}. Ces mécanismes sont décrits respectivement par la documentation Google Identity et la spécification JWT \cite{googleauth,jwt}. Les agents et administrateurs utilisent une session e-mail / mot de passe avec un rôle contrôlé par le backend.

\subsection{Actions du client}

Une fois authentifié, le client peut envoyer un message ou une pièce jointe, utiliser une action rapide, demander un agent humain, revenir vers l'IA, puis clôturer la conversation et évaluer sa satisfaction. Avec une conversation ouverte, le message est persisté ; si \texttt{handledBy = ia}, le service IA produit une réponse enregistrée comme message \texttt{ia}. Les messages et mises à jour sont émis sur Socket.io.

\subsection{Actions de l'agent humain}

Après authentification, l'agent consulte la file de conversations, prend en charge une conversation, répond au client, peut la rendre à l'IA et peut résoudre la conversation. Le passage au mode humain utilise \texttt{handledBy = humain} et \texttt{status = en\_attente}, puis l'agent traite le fil qui lui est attribué.

\subsection{Actions de l'administrateur}

L'administrateur authentifié consulte et filtre l'historique, valide ou supprime un agent et consulte les statistiques via les routes d'administration, notamment \texttt{GET /api/stats} et \texttt{GET /api/conversations}.

\section{Modèle de données}

MongoDB stocke quatre collections Mongoose. Les relations sont des références \texttt{ObjectId}.

\begin{table}[H]
\centering
\caption{Collection \texttt{users} (clients)}
\label{tab:user}
\begin{tabularx}{\textwidth}{|l|l|X|}
\hline
Champ & Type & Commentaire \\
\hline
googleId & String, unique, requis & Identifiant Google ou, en flux direct, souvent l'e-mail \\
\hline
name, email & String, requis & Profil affiché \\
\hline
avatar & String & URL d'avatar \\
\hline
timestamps & Date & \texttt{createdAt}, \texttt{updatedAt} \\
\hline
\end{tabularx}
\end{table}

\begin{table}[H]
\centering
\caption{Collection \texttt{agents} (opérateurs et administrateurs)}
\label{tab:agent}
\begin{tabularx}{\textwidth}{|l|l|X|}
\hline
Champ & Type & Commentaire \\
\hline
name, email & String (e-mail unique) & Identité de connexion \\
\hline
password & String & Hash bcrypt \\
\hline
role & \texttt{agent} $\mid$ \texttt{admin} & Défaut : agent \\
\hline
skills & [String] & Saisies à l'inscription ; \textbf{non utilisées} pour un routage automatique \\
\hline
status & \texttt{pending} $\mid$ \texttt{approved} & Un agent non approuvé ne peut pas se connecter \\
\hline
avatar & String & Optionnel \\
\hline
\end{tabularx}
\end{table}

\begin{table}[H]
\centering
\caption{Collection \texttt{conversations}}
\label{tab:conv}
\begin{tabularx}{\textwidth}{|l|l|X|}
\hline
Champ & Type & Commentaire \\
\hline
client & ObjectId $\rightarrow$ User & Propriétaire \\
\hline
assignedAgent & ObjectId $\rightarrow$ Agent & Peut être nul \\
\hline
status & \texttt{en\_cours} $\mid$ \texttt{en\_attente} $\mid$ \texttt{resolu} & Cycle de vie \\
\hline
handledBy & \texttt{ia} $\mid$ \texttt{humain} & Qui répond \\
\hline
satisfaction.rating & Number 1--5 & Optionnel à la clôture \\
\hline
satisfaction.comment & String & Optionnel \\
\hline
resolvedBy, resolvedAt & String, Date & Origine et instant de résolution \\
\hline
resolutionType & Enum & Résolution confirmée par le client, par l'agent ou abandon \\
\hline
lastActivityAt & Date & Sélection de la conversation active la plus récente \\
\hline
aiAttemptCount, escalationCount & Number & Suivi des tentatives IA et des escalades \\
\hline
isTestData & Boolean & Marqueur exclu des indicateurs opérationnels \\
\hline
\end{tabularx}
\end{table}

\begin{table}[H]
\centering
\caption{Collection \texttt{messages}}
\label{tab:msg}
\begin{tabularx}{\textwidth}{|l|l|X|}
\hline
Champ & Type & Commentaire \\
\hline
conversation & ObjectId & Fil parent \\
\hline
sender & \texttt{client} $\mid$ \texttt{ia} $\mid$ \texttt{humain} & Provenance \\
\hline
authorName & String & Affichage \\
\hline
content & String & Texte (peut être vide si pièce jointe) \\
\hline
attachments[] & url, type, name & \texttt{image} $\mid$ \texttt{video} $\mid$ \texttt{document} $\mid$ \texttt{link} \\
\hline
quickReplies[] & id, label, action, metadata & Actions proposées après une réponse IA \\
\hline
timestamps & Date & \texttt{createdAt}, \texttt{updatedAt}, utilisés pour l'affichage du fil \\
\hline
\end{tabularx}
\end{table}

\section{Diagramme de classes}

\begin{figure}[H]
\centering
\includesvg[width=\textwidth,keepaspectratio]{images/uml/domain-data}
\caption{Modèle de domaine et principales relations persistées de SINAPS}
\label{fig:classes}
\end{figure}

\section{Diagrammes de séquence}

\noindent Chaque cas d'utilisation important de SINAPS est représenté par son propre diagramme de séquence. Les scénarios sont regroupés selon l'acteur concerné afin de rendre lisibles les échanges métier, sans détailler les choix d'implémentation.

\subsection{Authentification}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/01-authentification.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{S'authentifier}}
\label{fig:seq-authentification}
\end{figure}

\subsection{Parcours client}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/02-envoyer-message-piece-jointe.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Envoyer un message ou une pièce jointe}}
\label{fig:seq-message}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/03-action-rapide.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Utiliser une action rapide}}
\label{fig:seq-action-rapide}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/04-demander-agent-humain.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Demander un agent humain}}
\label{fig:seq-demander-agent}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/05-revenir-vers-ia.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Revenir vers l'IA}}
\label{fig:seq-retour-ia-client}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/06-cloturer-evaluer.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Clôturer et évaluer}}
\label{fig:seq-cloture-evaluation}
\end{figure}

\subsection{Parcours agent humain}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/07-file-conversations.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Consulter la file de conversations}}
\label{fig:seq-file-conversations}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/08-prendre-en-charge.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Prendre en charge une conversation}}
\label{fig:seq-prise-en-charge}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/09-repondre-client.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Répondre au client}}
\label{fig:seq-reponse-client}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/10-resoudre-conversation.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Résoudre une conversation}}
\label{fig:seq-resolution}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/11-rendre-conversation-ia.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Rendre la conversation à l'IA}}
\label{fig:seq-retour-ia-agent}
\end{figure}

\subsection{Administration}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/12-valider-supprimer-agent.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Valider ou supprimer un agent}}
\label{fig:seq-validation-agent}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/13-historique-filtre.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Consulter et filtrer l'historique}}
\label{fig:seq-historique}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/14-statistiques.png}
\caption{Diagramme de séquence du cas d'utilisation \enquote{Consulter les statistiques}}
\label{fig:seq-statistiques}
\end{figure}

\section{Cycle de vie d'une conversation}

Une nouvelle demande du client démarre sous la prise en charge de l'IA. En revanche, lorsqu'une conversation active existante est retrouvée, elle est restaurée dans son état courant au lieu d'être réinitialisée. Les échanges avec l'IA se poursuivent tant que cette prise en charge est conservée. La figure~\ref{fig:conversation-state} synthétise les états et transitions décrits ci-dessous.

Le client peut demander un agent humain ; la conversation passe alors en attente jusqu'à ce qu'un agent la prenne en charge. Une conversation en attente peut être clôturée directement, tandis qu'une conversation traitée par un humain peut revenir vers l'IA par désescalade. Les échanges se poursuivent avec l'interlocuteur correspondant à l'état courant.

Une conversation peut être résolue depuis la prise en charge IA, depuis l'attente ou pendant le traitement humain. La résolution peut être confirmée par le client ou effectuée par l'agent lorsque le flux le prévoit ; une note de satisfaction et un commentaire peuvent alors être enregistrés. Les statuts et modes associés sont récapitulés dans l'annexe~\ref{tab:annexe-statuts}.

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth,height=0.68\textheight,keepaspectratio]{images/uml/sinaps-conversation-lifecycle.png}
\caption{Cycle de vie d'une conversation selon son statut et son responsable}
\label{fig:conversation-state}
\end{figure}

\section{Authentification et gestion des droits}

Deux familles de jetons JWT signés avec \texttt{JWT\_SECRET} coexistent.

\begin{table}[H]
\centering
\caption{Politique d'authentification}
\label{tab:auth}
\small
\begin{tabularx}{\textwidth}{|l|X|}
\hline
Profil & Mécanisme \\
\hline
Client & JWT \texttt{role=client}, 30~jours, après Google (ID token vérifié) ou identification e-mail. Stockage navigateur : \texttt{sinaps\_client}. \\
\hline
Agent / admin & JWT \texttt{role=agent$\mid$admin}, 7~jours, après e-mail + mot de passe. Refus si agent \texttt{pending}. Stockage : \texttt{sinaps\_token}. \\
\hline
Middleware & \texttt{requireAuth}, \texttt{requireAdmin}, \texttt{requireAgentOrAdmin}, \texttt{requireClientAuth}, \texttt{requireConversationAccess}, \texttt{requireSenderAuth}, \texttt{requireAnySession}. \\
\hline
\end{tabularx}
\end{table}

Les règles de conception sont les suivantes :

\begin{itemize}[leftmargin=1.4em]
  \item un client ne lit et ne modifie que \textbf{sa} conversation ;
  \item \texttt{sender = humain} exige un JWT agent/admin ;
  \item \texttt{sender = ia} est refusé s'il vient d'une requête externe ;
  \item l'upload exige une session client ou agent.
\end{itemize}

En production, le serveur refuse de démarrer sans \texttt{JWT\_SECRET}. La mise en œuvre détaillée des routes et des middleware est présentée au chapitre~4.

\section{Conception du RAG}

\subsection{Retriever}

La base \texttt{data/knowledgeBase.js} contient des paires \texttt{\{ question, answer \}}. Chaque document est normalisé et tokenisé, puis indexé en TF et pondéré par IDF. La requête et les documents sont comparés par \textbf{similarité cosinus}. Les extraits dont le score est strictement supérieur à $0{,}35$ sont retenus, avec un maximum de deux documents (\texttt{topK = 2}).

Ce n'est \textbf{pas} un RAG vectoriel : aucun embedding ni base vectorielle n'est utilisé. Il s'agit d'un RAG \og lexical \fg{} adapté à une petite FAQ de démonstration.

\subsection{Génération et repli}

Si une clé Gemini valide est présente, les extraits pertinents sont injectés dans un prompt français afin de produire une réponse courte et fondée sur la base de connaissances. Les modèles Gemini configurés sont essayés en cascade. Si la clé est absente, si l'appel échoue ou si aucun document pertinent n'est disponible, le système renvoie la meilleure réponse locale lorsqu'elle existe, sinon un message invitant à demander l'aide d'un agent humain.

\subsection{Escalade}

L'escalade n'est \textbf{pas automatique} selon un score de confiance. Elle est \textbf{décidée par l'utilisateur} ou par un acteur autorisé sur la conversation via l'action prévue à cet effet. Le mécanisme RAG peut conduire à proposer cette aide humaine lorsqu'aucune réponse pertinente n'est disponible, mais le changement de prise en charge reste une action explicite.

\section{Conclusion}

Ce chapitre a présenté l'architecture générale et logicielle, les cas d'utilisation, le modèle de données, les séquences, le cycle de vie des conversations, l'authentification et la conception du RAG. Le chapitre suivant décrit la réalisation concrète de ces choix dans les applications web, mobile et backend.
